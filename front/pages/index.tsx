import styled from 'styled-components';

const StyledHeader = styled.h1`
  color: red;
  font-weight: bold;
  font-size: 2rem;
`;

const Home = () => {
  return <StyledHeader>HI</StyledHeader>;
};

export default Home;
